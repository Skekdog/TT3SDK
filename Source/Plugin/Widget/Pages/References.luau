local Frame = require("../../../Lib/UI/Components/Frame")
local Widget = require("./..")

local references = Frame({
	Name = "References",
	Parent = Widget.Pages,
	IsContainer = true,
})

return references